# rustfmt 配置：2024 edition 风格，宽度 100
# newline_style=Auto：项目 git core.autocrlf=true，工作树为 CRLF；强制 Unix 会让 fmt --check 在每次 commit 失败
edition = "2024"
style_edition = "2024"
max_width = 100
newline_style = "Auto"
