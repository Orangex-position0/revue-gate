# rustfmt config: 2024 edition style, width 100
# newline_style=Auto: the repo sets git core.autocrlf=true so the worktree is CRLF; forcing Unix would make fmt --check fail on every commit
edition = "2024"
style_edition = "2024"
max_width = 100
newline_style = "Auto"
